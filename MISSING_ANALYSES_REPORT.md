# Missing analyses report

The two analyses previously identified as missing have been resolved from the
author-supplied manuscript definitions and existing record-level inputs.

- The ±20% analysis now perturbs each of the five static module weights once in
  each direction (ten scenarios), applies missing-aware renormalization, and
  reports target- and scenario-level stability tables.
- The native single-tool comparison now consolidates same-tool units, retains
  native output order, removes repeated mappings to the same cluster-v2 region,
  identifies the 14 five-tool complete cases, and reproduces manuscript Table
  1 metrics.

Both analyses are generated by the public pipeline, stored under
`results/reference/analysis/`, and covered by scientific snapshot tests.

Status: **no manuscript analysis is currently known to be missing from the
processed-data reproducibility scope**.
